import { LightningElement, api } from "lwc";

export default class packingArea1to7 extends LightningElement {
    @api recordId;
    currentChecklistId = "";
    packingArea = {};
    values = "";
    options = [];

    handleClick() {
        // do nothing
    }

    handleChange() {
        // do nothing
    }
}
